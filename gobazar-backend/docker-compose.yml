version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: gobazar_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: gobazar_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: gobazar_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  postgres_data:
